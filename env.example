# Gittensor Validator Environment Configuration
# Copy this file to .env and fill in your values
#
# Usage: cp env.example .env && nano .env

# ============================================
# REQUIRED SETTINGS
# ============================================

# Bittensor Network UID for Gittensor
NETUID=74

# Wallet configuration
WALLET_NAME=default
HOTKEY_NAME=default

# Weights & Biases API key for logging
# Get your key at: https://wandb.ai/authorize
WANDB_API_KEY=

# ============================================
# OPTIONAL SETTINGS
# ============================================

# Subtensor network (default: finney)
# Options: finney, test, local
SUBTENSOR_NETWORK=finney

# Validator axon port (default: 8099)
PORT=8099

# Logging level (default: info)
# Options: info, debug, trace
LOG_LEVEL=info

# Store validation results in database (default: false)
# Set to true if you want to persist results to Postgres
STORE_DB_RESULTS=false

# ============================================
# DATABASE SETTINGS (only if STORE_DB_RESULTS=true)
# ============================================

# Database connection settings
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=gittensor
# DB_USER=gittensor
# DB_PASSWORD=

# ============================================
# DOCKER SETTINGS
# ============================================

# Docker image to use (default: entrius/gittensor-validator)
DOCKER_IMAGE=entrius/gittensor-validator

# Docker image tag (default: latest)
# Options: latest, sha-<commit>, v1.0.0, etc.
DOCKER_TAG=latest

# Path to bittensor wallets on host (default: ~/.bittensor/wallets)
WALLET_PATH=~/.bittensor/wallets
